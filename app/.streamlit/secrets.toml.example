SUPABASE_URL = "https://cnjmuyctohfonygahxwq.supabase.co"
SUPABASE_KEY = "sb_publishable_JzJbRYnkz6AWFwYP5bOAeQ_6lTWEF1h"
# ^ publishable/anon key -- correct one to use here. RLS SELECT-only policies
# were added for the anon role on all six tables so this key can read but
# never write. Do NOT put the service_role key here.
#
# Copy this file to .streamlit/secrets.toml for local runs (and .gitignore
# secrets.toml). For Streamlit Community Cloud, paste these two lines into
# the app's Settings -> Secrets instead -- don't commit secrets.toml itself.
